config.output = config.output || {};
// Relative publicPath works for GitHub Pages project path (/ghost-serializer/)
// and for local wasmJsBrowserDevelopmentRun without a hardcoded base.
config.output.publicPath = "auto";
